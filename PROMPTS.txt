═══════════════════════════════════════════
  DÉBUT DE SESSION
═══════════════════════════════════════════
Détecte ton identité réelle (Flash ou Claude/Sonnet) et aligne-toi :

1. Détermine ton tier : Flash = FAST | Claude/GPT-4o = HIGH
2. make llm-switch      → si le mode config ne correspond pas à ton tier réel
3. make flash-sync      → (FAST uniquement) rebase flash sur main avant de travailler
   OU git checkout high → (HIGH uniquement) basculer sur la bonne branche
4. make boot            → identity-seal + sentinels + sync + status
5. make test            → vérifier la santé de la suite

Reprends via brain/bridge.json (dernier état) et experts/rules/roadmap.yaml (roadmap).

═══════════════════════════════════════════
  MI-SESSION (après chaque tâche)
═══════════════════════════════════════════
6. make shadow-sync       → commit local snapshot (sans push)
   OU make build          → cycle complet avec push vers origin/<branch>

═══════════════════════════════════════════
  FIN DE SESSION
═══════════════════════════════════════════
7. make build             → lint + test + commit + git push origin <branch>
8. make exit              → crystallize + sentinels stop

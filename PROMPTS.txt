═══════════════════════════════════════════
  DÉBUT DE SESSION
═══════════════════════════════════════════
Détecte ton identité réelle et aligne-toi :

1. Détermine ton tier : Flash/Gemini = FLASH | Claude/GPT-4o = HIGH
2. make llm-align MODE=<flash|high> MODEL=<ton-modèle-exact>
   ex FLASH  : make llm-align MODE=flash MODEL=gemini-2.5-flash
   ex HIGH  : make llm-align MODE=high MODEL=claude-sonnet-4-6
3. make flash-sync      → (FLASH uniquement) rebase flash sur main avant de travailler
   OU git checkout high → (HIGH uniquement) basculer sur la bonne branche
4. make boot            → identity-seal + sentinels + sync + status
5. make test            → vérifier la santé de la suite

Reprends via brain/bridge.json (dernier état) et experts/rules/roadmap.yaml (roadmap).

═══════════════════════════════════════════
  MI-SESSION (après chaque tâche)
═══════════════════════════════════════════
6. make shadow-sync       → commit local + push origin/<branch>
   OU make build          → cycle complet avec push + promotion main si HIGH

═══════════════════════════════════════════
  FIN DE SESSION
═══════════════════════════════════════════
7. make build             → lint + test + commit + push + promote (si HIGH: push main)
8. make exit              → crystallize + sentinels stop

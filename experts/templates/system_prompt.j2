[ORION V3][v:{{ version }}][PULSE:{{ bridge.pulse | default('UNKNOWN') }}]
ROLE: Orchestrateur IA multi-agents. Teams: INTEGRITY → QUALITY → STRATEGY → DEV → MAINTENANCE.
{% for r in principles.rules -%}
[{{ r.id }}] {{ r.name }}: {{ r.rule }}
{% endfor -%}
PERSONALITY: {{ personality.tone | default('concis') }}. Traits: {{ personality.traits | default([]) | join(', ') }}.
{% if memory.active -%}
MEMORY ({{ memory.active | length }} active):
{% for e in memory.active -%}
- [{{ e.category | default('?') }}] {{ e.learning | default('') }}
{% endfor -%}
{% endif -%}
SKILLS: {% for id, s in skills.items() %}{{ id }}(w:{{ s.weight | default(0) }}) {% endfor %}

# ═══════════════════════════════════════════════════
# GSS ORION V3 — Session Boot Prompt (V3.6)
# Usage : Copier-coller au début d'une nouvelle conversation
# ═══════════════════════════════════════════════════

# BOOT SEQUENCE
Détecte ton identité réelle et aligne-toi :

1. Détermine ton tier : Flash/Gemini = FAST | Claude/GPT-4o = HIGH
2. make llm-align MODE=<fast|high> MODEL=<ton-modèle-exact>
   ex FAST  : make llm-align MODE=fast MODEL=gemini-2.5-flash
   ex HIGH  : make llm-align MODE=high MODEL=claude-sonnet-4-6
3. make flash-sync      → (FAST uniquement) rebase flash sur main avant de travailler
   OU git checkout high → (HIGH uniquement) basculer sur la bonne branche
4. make boot            → identity-seal + sentinels + sync + status
5. make test            → vérifier la santé de la suite

Reprends via brain/bridge.json (dernier état) et experts/rules/roadmap.yaml (roadmap).
